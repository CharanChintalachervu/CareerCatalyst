# CareerCatalyst — Web (Next.js + TypeScript + Tailwind)

## Quick Start
```bash
npm install
cp .env.local.example .env.local
npm run dev
# http://localhost:3000
```

Set `NEXT_PUBLIC_API_BASE_URL` to your Express API (default http://localhost:8080).
